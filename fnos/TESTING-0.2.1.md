# FlClash 飞牛 0.2.1 测试记录

## 修复范围

- 修复 `sing-tun v0.4.21` 将结束于地址族最大值的排除区间编码为重复 nftables 键，导致透明代理启动时报 `netlink receive: file exists`。
- 保留 `240.0.0.0/4`、`ff00::/8` 及原有局域网排除范围；构建时应用上游已合并的等价修复，不切换到 iptables。
- 同一核心已经启动监听器后，后续配置应用不再重复调用启动接口。
- 网络范围确认文案根据“仅 IPv4”或“双栈”动态显示。

## 已完成验证

- 在 fnOS 1.2.0505 实机日志复现原错误，确认配置解析、节点加载完成，失败点位于 TUN 自动重定向 nftables 地址集合。
- `go test ./...`、`go vet ./...` 和前端自动化测试通过。
- 固定核心交叉编译成功，构建覆盖唯一匹配 `sing-tun v0.4.21`，产物保留固定 mihomo 提交。

## 待完成验证

- 0.2.1 安装升级、现有订阅和设置保留。
- 仅 IPv4 模式开启、关闭与连续三轮重试。
- TUN、路由表、nftables 表、DNS、管理入口及普通直连恢复。
- Docker、SMB、端口映射和虚拟机的实际流量验收仍须分别记录。
